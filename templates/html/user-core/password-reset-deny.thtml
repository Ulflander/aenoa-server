

<?php if ( !App::isAjax() ) : ?>
<div class="user-block">
<h2><?php echo _('Password reset denied') ?></h2>
<div id="usercore/register/container" class="expanded">
<?php endif; ?>

	<?php $this->renderElement('responses'); ?>

	<p><strong><?php echo sprintf(_('Password related to %s email address has not been reseted.'), $email) ?></strong></p>

<?php if ( !App::isAjax() ) : ?>
</div>
</div>
<?php endif; ?>
